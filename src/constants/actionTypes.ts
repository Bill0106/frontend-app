const ACTION_TYPES = {
  PENDING: 'PENDING',
  FETCHED: 'FETCHED',
  ERROR: 'ERROR',
};

export enum ActionType {
  Pending = 'PENDING',
  Fetched = 'FETCHED',
  Error = 'ERROR',
}

export default ACTION_TYPES;
